import styled from "styled-components";

export const LongforWrapper = styled.div`
  .longfor-list {
    /* 引入scrollView 组件后，内容交给了 scrollView 处理， 如果还设置这个控制的事 ScrollView */
    /* display: flex; */
    margin-left: -8px;
  }
`